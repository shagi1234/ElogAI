<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_login"
    app:startDestination="@id/fragmentLogin">

    <fragment
        android:id="@+id/fragmentLogin"
        android:name="com.selbiconsulting.elog.ui.login.login_page.FragmentLogin"
        android:label="fragment_login"
        tools:layout="@layout/fragment_login" >
        <action
            android:id="@+id/action_fragmentLogin_to_fragmentAlreadyLoggedIn"
            app:destination="@id/fragmentAlreadyLoggedIn" />
    </fragment>
    <fragment
        android:id="@+id/fragmentAlreadyLoggedIn"
        android:name="com.selbiconsulting.elog.ui.login.already_logged_in.FragmentAlreadyLoggedIn"
        android:label="FragmentAlreadyLoggedIn" />
</navigation>